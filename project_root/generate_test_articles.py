"""
Генератор тестовых статей для бенчмарка LLM-as-a-Judge.
Создаёт 15 развёрнутых статей в папке test_articles/
"""

import os

# Создаём папку для статей
os.makedirs('test_articles', exist_ok=True)

ARTICLES = {
    "01_python_async.txt": """Асинхронное программирование в Python: asyncio и многопоточность

Асинхронное программирование становится стандартом для современных веб-приложений и API-сервисов, где необходимо обрабатывать тысячи одновременных подключений. Библиотека asyncio, встроенная в Python с версии 3.4, предоставляет элегантный способ написания конкурентного кода без сложностей многопоточности.

Ключевое преимущество asyncio — кооперативная многозадачность. Вместо переключения контекста операционной системой, программист явно указывает точки, где задача может передать управление. Ключевое слово await сообщает интерпретатору: эта операция будет блокирующей, можно переключиться на другую задачу. Это особенно эффективно для I/O-bound операций — запросов к базам данных, HTTP-вызовов, чтения файлов.

Типичный пример: веб-скрапер, загружающий 100 страниц. Синхронный код выполнит запросы последовательно, тратя 50 секунд на сетевые задержки. Асинхронная версия с aiohttp загрузит все страницы параллельно за 3-5 секунд, используя один поток. Event loop опрашивает сокеты и пробуждает корутины, как только данные готовы.

FastAPI и Starlette построены на asyncio, обеспечивая производительность, сравнимую с Node.js и Go. Бенчмарки показывают 50,000+ запросов в секунду на одном ядре для простых эндпоинтов. База данных доступа через async драйверы (asyncpg для PostgreSQL, motor для MongoDB) избегает блокировки главного потока.

Однако asyncio не подходит для CPU-bound задач. Вычисления матриц или обработка изображений блокируют event loop, парализуя все корутины. Для таких задач нужны ProcessPoolExecutor или Celery. Правильный подход — комбинировать asyncio для I/O и multiprocessing для вычислений.
""",

    "02_git_best_practices.txt": """Git workflow: лучшие практики версионного контроля в командной разработке

Система контроля версий Git стала индустриальным стандартом, но эффективное использование требует понимания рабочих процессов и соглашений. Хаотичная история коммитов, конфликты слияния и потерянные изменения — частые проблемы команд без чёткого git-workflow.

Git Flow, предложенный Vincent Driessen в 2010 году, остаётся популярным для проектов с регулярными релизами. Основные ветки: master (production), develop (интеграция), плюс временные feature, release и hotfix. Каждая фича разрабатывается в отдельной ветке, затем сливается в develop через pull request с code review. Релиз создаётся из develop, тестируется в release-ветке и мержится в master с тегом версии.

GitHub Flow упрощает процесс для continuous deployment: одна главная ветка main, все изменения через короткоживущие feature-ветки. Разработчик создаёт ветку, коммитит изменения, открывает PR, получает ревью и мержит в main. CI/CD пайплайн автоматически деплоит в production. Этот подход подходит веб-сервисам без фиксированных релизов.

Качество коммитов критично для поддерживаемости. Правило: один коммит = одно логическое изменение. Сообщение должно описывать "что" и "зачем", а не "как". Conventional Commits стандартизирует формат: feat, fix, docs, style, refactor, test, chore. Инструменты вроде commitlint проверяют соблюдение формата.

Rebase vs merge — вечный спор. Merge сохраняет полную историю, но создаёт merge-коммиты, загромождающие граф. Rebase перемещает коммиты, создавая линейную историю, но переписывает SHA, что опасно для публичных веток. Золотое правило: rebase для локальных веток, merge для интеграции в shared branches.
""",

    "03_docker_containers.txt": """Контейнеризация приложений: Docker и оркестрация микросервисов

Docker революционизировал развертывание приложений, решив проблему "работает на моей машине". Контейнеры упаковывают приложение со всеми зависимостями в изолированное окружение, гарантируя идентичное поведение на разработке, тестировании и production.

В отличие от виртуальных машин, контейнеры разделяют ядро операционной системы хоста, используя Linux namespaces и cgroups для изоляции. Это делает их невероятно лёгкими — контейнер занимает мегабайты против гигабайт для VM, стартует за секунды против минут. На одном сервере можно запустить сотни контейнеров.

Dockerfile описывает образ слоями: базовый образ (FROM python:3.11), установка зависимостей (RUN pip install), копирование кода (COPY), точка входа (CMD). Docker кэширует слои, ускоряя повторные сборки. Best practice — размещать редко меняющиеся команды в начале, часто меняющиеся (код) в конце.

Docker Compose оркеструет многоконтейнерные приложения. YAML-файл описывает сервисы: веб-приложение, база данных, Redis, очереди. Одна команда docker-compose up поднимает всю инфраструктуру локально, воспроизводя production-окружение. Разработчики получают изолированные среды без конфликтов зависимостей.

В production-масштабе используется Kubernetes — система оркестрации, автоматически распределяющая контейнеры по кластеру серверов. Kubernetes обеспечивает самовосстановление (перезапуск упавших контейнеров), масштабирование (добавление реплик под нагрузкой), балансировку трафика. Но сложность Kubernetes оправдана только для крупных систем — стартапам достаточно Docker Compose или managed-сервисов вроде AWS ECS.
""",

    "04_react_hooks.txt": """React Hooks: функциональные компоненты и управление состоянием

React Hooks, представленные в версии 16.8, фундаментально изменили разработку на React, позволив использовать state и lifecycle методы в функциональных компонентах. Это упростило код, устранив необходимость в классовых компонентах и их многословном синтаксисе.

useState — базовый хук для локального состояния. Вызов const [count, setCount] = useState(0) создаёт переменную состояния и функцию для её обновления. В отличие от this.setState в классах, useState может хранить любой тип — числа, строки, объекты, массивы. Множественные вызовы useState создают независимые переменные состояния.

useEffect заменяет componentDidMount, componentDidUpdate и componentWillUnmount одним API. Эффект выполняется после каждого рендера, а массив зависимостей контролирует, когда эффект перезапускается. Пустой массив [] означает выполнение только при монтировании. Возврат cleanup-функции эквивалентен componentWillUnmount.

Продвинутые хуки решают специфические задачи. useContext избегает prop drilling, передавая данные глубоко вложенным компонентам. useReducer управляет сложным состоянием с множеством переходов, альтернатива useState для state machines. useMemo и useCallback оптимизируют производительность, мемоизируя вычисления и функции.

Кастомные хуки инкапсулируют переиспользуемую логику. Хук useLocalStorage синхронизирует состояние с браузерным хранилищем, useFetch абстрагирует API-запросы с loading и error состояниями. Экосистема npm содержит тысячи готовых хуков для аутентификации, форм, анимаций.

Правила хуков строги: вызывать только на верхнем уровне (не в циклах/условиях), только в функциональных компонентах или кастомных хуках. ESLint-плагин eslint-plugin-react-hooks автоматически проверяет соблюдение правил.
""",

    "05_sql_optimization.txt": """Оптимизация SQL-запросов: индексы, планы выполнения и производительность баз данных

Медленные SQL-запросы — частая причина проблем производительности веб-приложений. Запрос, выполняющийся миллисекунды на тестовых данных, может работать секунды на миллионах записей production. Понимание индексации и планов выполнения критично для масштабируемых систем.

Индексы — структуры данных (обычно B-tree), ускоряющие поиск строк. WHERE, JOIN и ORDER BY выигрывают от индексов на соответствующих колонках. Но индексы замедляют INSERT/UPDATE, требуют дискового пространства. Баланс между скоростью чтения и записи определяет стратегию индексации.

EXPLAIN ANALYZE показывает план выполнения запроса: какие индексы использованы, сколько строк просканировано, время каждого шага. Sequential Scan (полное сканирование таблицы) сигнализирует об отсутствующем индексе. Index Scan эффективен, но Index Only Scan лучше — данные читаются из индекса без обращения к таблице.

N+1 проблема — классическая ловушка ORM. Загрузка 100 статей с авторами генерирует 101 запрос: 1 для статей, 100 для авторов. Решение — JOIN или eager loading, загружающий связанные данные одним запросом. В Django: Article.objects.select_related('author'), в SQLAlchemy: joinedload.

Денормализация жертвует нормальными формами ради производительности. Дублирование счётчика комментариев в таблице статей избегает COUNT(*) при каждом отображении. Материализованные представления предвычисляют тяжёлые агрегации. Партиционирование разбивает огромные таблицы по датам или диапазонам.

Мониторинг slow query log выявляет проблемные запросы в production. Инструменты вроде pgBadger анализируют логи PostgreSQL, показывая топ медленных запросов, частоту выполнения, временные тренды. Query планировщик базы данных иногда выбирает неоптимальный план — ANALYZE обновляет статистику таблиц, улучшая решения планировщика.
""",

    "06_rest_api_design.txt": """Проектирование RESTful API: принципы, версионирование и документация

REST (Representational State Transfer) архитектурный стиль для веб-API, использующий HTTP методы и статус-коды для манипуляции ресурсами. Хорошо спроектированный API интуитивен, консистентен и легко документируется, плохой — источник фрустрации для разработчиков.

Ресурсы идентифицируются URL-путями, представляющими существительные, не глаголы. GET /users/123 получает пользователя, POST /users создаёт, PUT /users/123 обновляет, DELETE /users/123 удаляет. Вложенные ресурсы: GET /users/123/posts для постов пользователя. Избегайте глаголов типа /getUser или /createPost.

HTTP статус-коды передают результат операции. 200 OK для успешного GET, 201 Created для POST, 204 No Content для DELETE. Ошибки: 400 Bad Request для невалидных данных, 401 Unauthorized без токена, 403 Forbidden с токеном но без прав, 404 Not Found для несуществующих ресурсов, 500 Internal Server Error для сбоев сервера.

Пагинация обязательна для коллекций. Возврат всех миллионов пользователей перегружает сеть и память. Параметры limit и offset: GET /users?limit=20&offset=40. Альтернатива — cursor-based pagination, более эффективная для больших датасетов. Заголовки Link (RFC 5988) указывают URL следующей/предыдущей страниц.

Версионирование предотвращает breaking changes. Варианты: в URL (/v1/users), в заголовке (Accept: application/vnd.api.v1+json), в параметре (?version=1). URL-версионирование наиболее явное и кэшируемое. Семантическое версионирование (major.minor.patch) коммуницирует обратную совместимость.

OpenAPI (Swagger) спецификация описывает API машиночитаемым YAML/JSON. Инструменты генерируют интерактивную документацию, клиентские SDK, валидацию запросов. Postman коллекции позволяют тестировать API вручную. Автогенерация спецификации из кода (FastAPI, NestJS) гарантирует актуальность документации.
""",

    "07_linux_performance.txt": """Мониторинг производительности Linux: инструменты диагностики и оптимизации

Диагностика узких мест производительности в Linux требует понимания метрик системы и специализированных утилит. Симптомы — медленный отклик приложений, высокая нагрузка, зависания — могут иметь десятки причин от перегрузки CPU до исчерпания дискового I/O.

top и htop показывают процессы в реальном времени: использование CPU, памяти, время работы. Load average — средняя длина очереди готовых к выполнению процессов за 1, 5, 15 минут. Значение выше количества CPU-ядер сигнализирует о перегрузке. us (user) и sy (system) показывают время в пользовательском и ядерном режимах, wa (iowait) — простой в ожидании I/O.

iostat детализирует дисковую активность: количество операций чтения/записи в секунду, пропускная способность в MB/s, утилизация диска в процентах. 100% утилизация означает, что диск постоянно занят — узкое место. Параметр await показывает среднее время отклика дисковых запросов; высокие значения (>10ms для SSD, >20ms для HDD) указывают на проблемы.

vmstat мониторит виртуальную память: свободная RAM, swap использование, скорость paging. Активный swap (si/so колонки) критичен — система меняет страницы памяти на диск, катастрофически замедляя работу. Решение — увеличить RAM или оптимизировать потребление памяти приложениями.

netstat и ss анализируют сетевые соединения: активные TCP-подключения, listening порты, статистику интерфейсов. Большое количество TIME_WAIT сокетов после нагрузочных тестов нормально, но может исчерпать ephemeral порты. Параметры ядра net.ipv4.tcp_tw_reuse и tcp_fin_timeout оптимизируют повторное использование сокетов.

perf — мощный профайлер, использующий аппаратные счётчики CPU. perf top показывает функции, потребляющие больше всего циклов процессора. perf record собирает данные, perf report визуализирует flame graphs. Идентификация горячих участков кода позволяет сфокусировать оптимизацию.
""",

    "08_machine_learning_datasets.txt": """Подготовка данных для машинного обучения: очистка, нормализация и feature engineering

Качество данных определяет успех моделей машинного обучения. Афоризм "garbage in, garbage out" особенно актуален — даже лучшие алгоритмы бесполезны на грязных данных. Подготовка датасетов занимает 80% времени data scientist, но критична для точности предсказаний.

Отсутствующие значения (missing values) требуют стратегии обработки. Удаление строк с пропусками теряет данные, допустимо только если пропусков мало. Заполнение средним/медианным значением работает для числовых фич. Для категориальных — создание отдельной категории "unknown". Продвинутый подход — предсказание пропущенных значений моделью.

Выбросы (outliers) искажают статистику и веса моделей. Визуализация boxplot или scatter plot выявляет аномальные точки. Z-score метод удаляет значения, отклоняющиеся на 3+ стандартных отклонения от среднего. IQR (Interquartile Range) метод отсекает за пределами 1.5*IQR от квартилей. Но выбросы могут быть легитимными — анализ бизнес-контекста обязателен.

Нормализация и стандартизация приводят фичи к общему масштабу. Min-max scaling масштабирует в диапазон [0,1], сохраняя распределение. Standardization (z-score) центрирует со средним 0 и стандартным отклонением 1, предполагая нормальное распределение. Модели вроде linear regression, SVM, neural networks чувствительны к масштабу; деревья решений — нет.

Feature engineering создаёт новые признаки из существующих. Из даты извлекаются день недели, месяц, является ли выходным. Логарифмирование сжимает распределения с длинным хвостом. Бинаризация превращает числа в категории (возраст → возрастная группа). Взаимодействия (interaction features) комбинируют признаки (площадь * этаж для цены квартиры).

Кодирование категориальных переменных обязательно, так как большинство алгоритмов работают с числами. One-hot encoding создаёт бинарную колонку для каждой категории. Label encoding присваивает числа, но вводит ложную упорядоченность. Target encoding заменяет категорию средним целевой переменной, но требует осторожности с overfitting.
""",

    "09_webpack_bundlers.txt": """Сборщики модулей: Webpack, Vite и оптимизация фронтенд-приложений

Современные веб-приложения состоят из сотен JavaScript-модулей, стилей, изображений. Сборщики (bundlers) анализируют зависимости, объединяют файлы, применяют трансформации и оптимизации, выдавая production-ready бандлы для браузера.

Webpack доминировал годами благодаря гибкости и экосистеме плагинов. Конфигурация определяет entry points, loaders для трансформации файлов (babel-loader для JSX, css-loader для стилей), plugins для оптимизации. Code splitting разбивает бандл на чанки, загружаемые по требованию — критично для производительности крупных SPA.

Tree shaking удаляет неиспользуемый код, анализируя ES6 import/export. Если модуль экспортирует 10 функций, но используется 1, остальные 9 исключаются из бандла. Это требует ES модулей (не CommonJS) и минификатора вроде Terser. Реальные проекты экономят 30-50% размера бандла.

Vite — новое поколение tooling, использующее нативные ES модули браузера в dev-режиме. Вместо бандлинга всего приложения при старте, Vite сервирует модули по запросу, обеспечивая мгновенный cold start даже для огромных проектов. Hot Module Replacement обновляет изменённые модули за миллисекунды. Production сборка использует Rollup.

Lazy loading компонентов уменьшает initial bundle size. React.lazy(() => import('./Component')) загружает компонент только при рендере. Route-based splitting — каждая страница в отдельном чанке. Suspense обрабатывает loading состояние. Анализ coverage в Chrome DevTools показывает неиспользуемый код на текущей странице.

Asset optimization сжимает изображения, шрифты, SVG. image-webpack-loader применяет mozjpeg, pngquant автоматически. WebP формат обеспечивает 30% меньший размер при той же качестве. Content Hash в именах файлов (app.a3f2b1.js) инвалидирует кэш при изменениях. CDN доставляет статику географически близким сервером, снижая latency.
""",

    "10_redis_caching.txt": """Кэширование с Redis: стратегии, структуры данных и производительность

Redis — in-memory база данных, используемая для кэширования, очередей сообщений, pub/sub, session storage. Хранение данных в оперативной памяти обеспечивает субмиллисекундные задержки, на порядки быстрее дисковых баз данных. Типичные use cases — кэш результатов дорогих запросов, счётчики, временные данные.

Cache-aside паттерн: приложение проверяет кэш, при промахе запрашивает базу данных, сохраняет в кэш. GET /users/123 сначала пытается redis.get('user:123'), при отсутствии запрашивает PostgreSQL, затем redis.set('user:123', data, ex=3600) с TTL 1 час. Это сокращает нагрузку на БД на 90%+ для часто читаемых данных.

Write-through кэш синхронно записывает в БД и кэш при каждом обновлении. Это гарантирует консистентность, но замедляет записи. Write-behind (write-back) асинхронно сбрасывает изменения, ускоряя записи, но рискует потерей при крашах. Выбор зависит от tolerance к несогласованности и latency requirements.

Redis поддерживает сложные структуры данных. Strings для простых пар ключ-значение. Hashes для объектов (HSET user:123 name "John"). Lists для очередей (LPUSH, RPOP). Sets для уникальных элементов (SADD, SMEMBERS). Sorted Sets для рейтингов (ZADD, ZRANGE). Использование правильной структуры критично для производительности.

Eviction policies определяют, что удалять при заполнении памяти. LRU (Least Recently Used) вытесняет редко используемые ключи. LFU (Least Frequently Used) учитывает частоту обращений. TTL (Time To Live) автоматически удаляет просроченные ключи. volatile-lru применяет LRU только к ключам с TTL, защищая постоянные данные.

Redis Cluster распределяет данные по шардам для горизонтального масштабирования. 16384 hash slot'ов равномерно распределены между нодами. Клиент перенаправляется на правильный шард по hash ключа. Репликация обеспечивает отказоустойчивость — master реплицирует данные на slaves, failover автоматически промотит slave при падении master.
""",

    "11_typescript_generics.txt": """TypeScript: обобщённые типы и продвинутое типизирование

TypeScript добавляет статическую типизацию к JavaScript, предотвращая целые классы ошибок на этапе компиляции. Обобщённые типы (generics) позволяют писать переиспользуемый код, работающий с разными типами данных, сохраняя type safety.

Простейший generic — функция identity<T>(arg: T): T возвращает аргумент того же типа. Вызов identity<string>("hello") конкретизирует T как string, вызов identity<number>(42) — как number. Компилятор выводит тип автоматически: identity("hello") неявно понимает T=string.

Generic интерфейсы описывают структуры данных. Interface Array<T> в стандартной библиотеке позволяет Array<number>, Array<string>, Array<User>. Custom интерфейс Response<T> { data: T; status: number } типизирует API-ответы: Response<User[]> для списка пользователей, Response<Post> для поста.

Ограничения (constraints) сужают допустимые типы. <T extends HasId> требует, чтобы T имел свойство id. Функция findById<T extends HasId>(items: T[], id: number) работает с любыми объектами с id. Это обеспечивает доступ к свойствам T без потери type safety.

Utility types упрощают трансформации типов. Partial<T> делает все свойства опциональными, полезно для update-функций. Required<T> наоборот, делает обязательными. Pick<T, K> выбирает подмножество свойств. Omit<T, K> исключает. Record<K, V> создаёт объект с ключами типа K и значениями V.

Mapped types итерируют по свойствам типа. Type ReadOnly<T> = { readonly [P in keyof T]: T[P] } делает все свойства read-only. Conditional types вида T extends U ? X : Y применяют логику. NonNullable<T> = T extends null | undefined ? never : T удаляет null.

Template literal types конструируют строковые литералы. Type EventName<T> = `on${Capitalize<T>}` превращает "click" в "onClick". Это мощно для строго типизированных event handlers, CSS классов, API endpoints. Inference в conditional types извлекает типы из структур вроде Promise<T> → T.
""",

    "12_nginx_configuration.txt": """Конфигурация Nginx: reverse proxy, балансировка нагрузки и оптимизация

Nginx — высокопроизводительный веб-сервер и reverse proxy, обрабатывающий десятки тысяч одновременных подключений с минимальным потреблением памяти. Event-driven архитектура превосходит Apache в конкурентности, делая Nginx стандартом для современных веб-стеков.

Reverse proxy терминирует клиентские подключения и проксирует запросы к backend-серверам. Конфигурация location / { proxy_pass http://localhost:3000; } перенаправляет трафик на Node.js приложение. Nginx обрабатывает SSL, сжатие, статику, освобождая приложение для бизнес-логики.

Load balancing распределяет запросы между несколькими backend-серверами. Upstream блок определяет пул серверов: upstream app { server app1:3000; server app2:3000; server app3:3000; }. Алгоритм round-robin циклически перебирает серверы, least_conn направляет на наименее загруженный, ip_hash обеспечивает sticky sessions.

Health checks исключают упавшие серверы из балансировки. Параметры max_fails=3 fail_timeout=30s временно блокируют сервер после трёх ошибок. Nginx Plus поддерживает активные health checks с кастомными HTTP-запросами. Open source версия полагается на пассивные проверки при обработке реальных запросов.

Кэширование статики разгружает backend. proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=my_cache:10m max_size=1g задаёт параметры кэша. proxy_cache my_cache в location включает кэширование. Заголовки Cache-Control и Expires контролируют TTL. Кэш-ключ по умолчанию — URL, настраивается proxy_cache_key.

Gzip сжатие экономит трафик. gzip on; gzip_types text/css application/javascript; сжимает текстовые ресурсы на лету. Современные клиенты поддерживают Brotli, обеспечивающий лучшую компрессию. Rate limiting (limit_req_zone) защищает от DDoS, ограничивая запросы с IP.

SSL/TLS termination снимает нагрузку шифрования с приложения. ssl_certificate и ssl_certificate_key указывают сертификаты Let's Encrypt. Современные протоколы TLSv1.2 и TLSv1.3 с secure ciphers защищают от атак. HTTP/2 multiplexing улучшает производительность, включается директивой listen 443 ssl http2.
""",

    "13_pytest_testing.txt": """Тестирование Python-приложений: pytest, fixtures и coverage

Автоматизированное тестирование — фундамент надёжного ПО. Pytest — наиболее популярный фреймворк для Python, сочетающий простоту использования с мощными возможностями. В отличие от unittest, pytest требует минимум boilerplate, использует обычные assert вместо специальных методов.

Простейший тест — функция с префиксом test_: def test_addition(): assert add(2, 3) == 5. Pytest автоматически обнаруживает тестовые файлы (test_*.py, *_test.py) и функции. Запуск pytest в терминале выполняет все тесты, показывая зелёные точки для успешных, F для падений.

Fixtures предоставляют тестовым функциям переиспользуемые ресурсы. @pytest.fixture декоратор превращает функцию в фикстуру: @pytest.fixture def db(): database = setup_database(); yield database; database.close(). Тест принимает фикстуру как аргумент: def test_query(db): assert db.query("SELECT 1") == 1.

Область видимости (scope) контролирует частоту создания фикстур. Scope="function" (по умолчанию) создаёт новую фикстуру для каждого теста, обеспечивая изоляцию. Scope="module" или "session" создают единожды для модуля/сессии, экономя время на дорогих операциях вроде подключения к БД.

Parametrize запускает тест с множеством входных данных. @pytest.mark.parametrize("input,expected", [(2,4), (3,9), (4,16)]) def test_square(input, expected): assert square(input) == expected. Один тест генерирует три отдельных выполнения. Это компактнее дублирования кода и улучшает диагностику при падениях.

Coverage измеряет процент кода, покрытого тестами. Pytest-cov плагин интегрирует coverage.py: pytest --cov=myapp --cov-report=html. HTML-отчёт визуализирует покрытые и непокрытые строки. 80%+ coverage — хороший показатель, но 100% не гарантирует отсутствие багов.

Mocking изолирует тестируемый код от внешних зависимостей. unittest.mock.patch подменяет функции/объекты: with patch('requests.get') as mock_get: mock_get.return_value.json.return_value = {"data": "test"}. Тесты API-клиентов не делают реальных HTTP-запросов, ускоряясь и избегая флапов.
""",

    "14_css_flexbox_grid.txt": """Современная CSS-вёрстка: Flexbox и Grid Layout

CSS прошёл путь от таблиц и float'ов к мощным layout-системам Flexbox и Grid. Эти модули декларативно описывают расположение элементов, автоматически обрабатывая responsive дизайн и выравнивание без хаков.

Flexbox — одномерная система для распределения элементов вдоль главной оси (row или column). display: flex на контейнере активирует flex-контекст. justify-content выравнивает по главной оси (flex-start, center, space-between), align-items — по поперечной. flex: 1 на дочернем элементе заставляет его занимать свободное пространство.

Типичный use case — navbar с логотипом слева и меню справа. .navbar { display: flex; justify-content: space-between; } автоматически распределяет элементы по краям. flex-wrap: wrap переносит элементы на новую строку при нехватке места, реализуя responsive grid без media queries.

CSS Grid — двумерная система для сложных layout'ов. grid-template-columns: 200px 1fr 1fr определяет колонки: фиксированная sidebar 200px, две равные колонки контента. grid-template-rows задаёт строки. gap: 20px устанавливает промежутки между ячейками.

Grid areas именуют области layout'а. grid-template-areas: "header header" "sidebar main" "footer footer" визуально описывает структуру. Элементы размещаются grid-area: header. Это интуитивнее числовых координат для сложных дизайнов.

Автоматическое размещение упрощает карточные сетки. grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)) создаёт адаптивную сетку: минимум 250px на карточку, максимум доступного пространства. auto-fill создаёт столько колонок, сколько влезает, auto-fit схлопывает пустые.

Responsive дизайн комбинирует Flexbox/Grid с media queries. @media (max-width: 768px) { flex-direction: column; } переключает layout на мобильных. clamp(1rem, 2.5vw, 2rem) — fluid typography, масштабирующийся между минимумом и максимумом.
""",

    "15_agile_scrum.txt": """Agile-методологии: Scrum, Kanban и управление разработкой

Agile-подход произвёл революцию в управлении проектами разработки ПО, заменив тяжеловесный Waterfall итеративными циклами с постоянной обратной связью. Agile Manifesto приоритизирует людей над процессами, работающий софт над документацией, сотрудничество над контрактами.

Scrum структурирует работу в спринты — 2-недельные итерации с фиксированным scope. Planning meeting в начале спринта превращает User Stories из backlog'а в задачи. Daily standup — 15-минутная синхронизация: что сделано вчера, планы на сегодня, блокеры. Retrospective в конце спринта анализирует процесс, выявляя улучшения.

User Stories описывают функциональность с точки зрения пользователя: "Как [роль], я хочу [действие], чтобы [выгода]". Например, "Как покупатель, я хочу фильтровать товары по цене, чтобы найти доступные опции". Acceptance criteria определяют условия выполнения, testable и однозначные.

Story points оценивают сложность относительно, не в часах. Команда использует Planning Poker: каждый оценивает задачу по Fibonacci (1,2,3,5,8,13), обсуждаются расхождения. Velocity — количество points, выполняемых за спринт, стабилизируется через 3-4 спринта, позволяя прогнозировать сроки.

Kanban визуализирует workflow на доске с колонками: Backlog, To Do, In Progress, Review, Done. WIP (Work In Progress) лимиты ограничивают задачи в колонках, предотвращая multitasking. Метрики: lead time (от начала до завершения), cycle time (от старта работы до done). Kanban подходит поддержке с непрерывным потоком задач, в отличие от sprint-based Scrum.

Continuous Integration/Deployment интегрирует в Agile. Автоматические тесты на каждый commit, деплой в production несколько раз в день. Feature flags включают функциональность для подмножества пользователей, минимизируя риски. Trunk-based development сливает в main ежедневно, избегая long-lived веток.
"""
}

def main():
    print("🚀 Генерация тестовых статей для бенчмарка")
    print("=" * 60)

    for filename, content in ARTICLES.items():
        filepath = os.path.join('test_articles', filename)
        with open(filepath, 'w', encoding='utf-8') as f:
            f.write(content)

        # Подсчёт символов
        char_count = len(content)
        word_count = len(content.split())

        print(f"✅ {filename}")
        print(f"   └─ {char_count:,} символов, {word_count:,} слов")

    print("\n" + "=" * 60)
    print(f"✨ Создано {len(ARTICLES)} статей в папке 'test_articles/'")
    print(f"📊 Общий объём: {sum(len(c) for c in ARTICLES.values()):,} символов\n")

if __name__ == "__main__":
    main()
